# Prefer: python install.py   (or install.sh / install.bat)
# This file lists non-torch deps; torch is chosen by the installer (GPU/CPU).
# For manual pip install into an existing venv you can also use:
#   pip install -r .requirements.txt
openai-clip
opencv-python-headless
Pillow
numpy
matplotlib
gradio>=4.0
setuptools<81
